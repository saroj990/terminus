{
  "name": "@lca/evals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "eval": "node --import tsx src/run.ts",
    "test": "node --import tsx src/run.ts"
  },
  "dependencies": {
    "@lca/agent-core": "workspace:*",
    "@lca/llm": "workspace:*",
    "@lca/policy": "workspace:*",
    "@lca/tools": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
